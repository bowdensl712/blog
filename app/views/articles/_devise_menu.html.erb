<div class='devise_menu'>
  <%= t('username') %>
  <%= current_user.email.truncate(17) %><br><br>
  <%= t('userCreatedDate') %>
  <%= current_user.created_at.strftime('%Y/%m/%d')%>
  <%= link_to t('logout'), destroy_user_session_path, data: { turbo_method: :delete }, class: 'devise_logout' %>
</div>
