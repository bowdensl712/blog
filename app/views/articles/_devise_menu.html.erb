<div class='devise_menu'>
  <%= t('username') %>
  <%= current_user.email.truncate(17) %><br><br>
  <%= t('userCreatedDate') %>
  <%= current_user.created_at.strftime('%Y/%m/%d')%>
  <div class='button_container'>
    <%= link_to t('logout'), destroy_user_session_path, data: { turbo_method: :delete }, class: 'devise_menu_button' %><br><br>
    <%= link_to t('editAccount'), edit_registration_path(current_user), class: 'devise_menu_button' %>
  </div>
</div>
